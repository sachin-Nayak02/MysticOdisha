<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Festival</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="row">
    <div class="col-8 offset-2">
    <h3>Create a new place</h3>
    <form action="/festival/<%= result._id %>?_method=PUT" method="post" novalidate class="needs-validation">
        <div class="mb-3">
            <label for="name" class="form-label">Festival Name</label>
            <input type="text" placeholder="Enter Festival Name here" name="name" id="name" class="form-control" required value="<%= result.name %>">
            <div class="valid-feedback">Looks Good</div>
            <div class="invalid-feedback"> name should be valid</div>
        </div>
     
        <div class="mb-3">
            <label for="season" class="form-label">season</label>
            <input required type="text" class="form-control" name="season" id="season" placeholder="Enter Season" value="<%= result.season %>" >
            <div class="invalid-feedback"> Season should be valid</div>
            <div class="valid-feedback">Looks Good</div>
    
        </div>
     
        <div class="mb-3">
            <label for="image" class="form-label">Image URL</label>
            <input type="text" placeholder="paste image URL/link here" name="image" 
            id="image" class="form-control" value="<%= result.image %>">
    
        </div>
        
        <div class="mb-3 col-md-4">
            <label for="Bio" class="form-label">Bio/Description</label>
            <textarea cols="9" rows="5"  id="Bio"  name="bio" class="form-control"  required ><%=result.bio %>
            </textarea> 
            <div class="invalid-feedback">Enter valid Bio</div>
            <div class="valid-feedback">Looks Good</div>
        </div>
    
        <button class="btn btn-outline-danger add-btn">Edit</button>
        
      
    </form>
    <br>
</body>
</html>