<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Festivals</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="head">
        <a href="http://localhost:3030/addFestival" class="btn btn-outline-info addbtn">Add Festivals</a>
        <form action="">
        <input type="search" class="searchPlace " name="season" placeholder="Search Season..">
        <button class="btn btn-outline-info">Search</button>
    </form>
    </div>
    
    <br><br>

    <div class="all">
    <% for(let festival of festivals){ %>
        <div class="card">
            <img src="<%= festival.image %>">
            
            <h3><%= festival.name %></h3>
            <p> <%= festival.season %> </p>
            <p> <%= festival.bio %> </p>

            <div class="btns">
                <a href="/festival/<%=festival._id%>/edit" class="btn btn-outline-success col-3 offset-1 edit-btn" >Edit</a>
            
            
            
            <form method="POST" action="/festival/<%= festival._id %>?_method=DELETE">
                <button class="btn btn-outline-danger offset-7">Delete</button>
               </form>
        </div>
        <br>
        </div>
    <% } %>
</div>

</body>
</html>
